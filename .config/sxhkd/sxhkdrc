alt + Return
	kitty --single-instance

super + space
	rofi -show run -font "consolas 20"

super + Escape
  ~/.bin/screenshot

super + Return
	killall karmen

alt + super + k
  amixer set 'Master' 5%+

alt + super + j
  amixer set 'Master' 5%-